# Generated for Konflux by openshift-pipelines/hack. DO NOT EDIT
name: update-sources-tektoncd-pruner

on:
  workflow_dispatch: {}
  schedule:
  - cron: "0 1 * * *" # At 1AM everyday

jobs:

  update-sources:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
    - name: Checkout the current repo
      uses: actions/checkout@v4
      with:
        ref: tektoncd-pruner

    - name: Clone tektoncd/pruner
      run: |
        rm -fR tektoncd-pruner
        git clone https://github.com/tektoncd/pruner tektoncd-pruner
        pushd tektoncd-pruner
        git checkout -B main origin/main
        popd
    - name: Commit new changes
      run: |
        git config user.name openshift-pipelines-bot
        git config user.email pipelines-extcomm@redhat.com
        git checkout -b actions/update/sources-tektoncd-pruner
        touch head
        pushd upstream
        OLD_COMMIT=$(cat ../head)
        NEW_COMMIT=$(git rev-parse HEAD)
        echo Previous commit: ${OLD_COMMIT}
        git show --stat ${OLD_COMMIT}
        echo New commit: ${NEW_COMMIT}
        git show --stat ${NEW_COMMIT}
        git diff --stat ${NEW_COMMIT}..${OLD_COMMIT} > /tmp/diff.txt
        git rev-parse HEAD > ../head
        popd
        rm -rf upstream/.git
        git add -f upstream head .konflux

        if [[ -z $(git status --porcelain --untracked-files=no) ]]; then
          echo "No change, exiting"
          exit 0
        fi

        git commit -F- <<EOF
        [bot] Update tektoncd-pruner from tektoncd/pruner to ${NEW_COMMIT}

            $ git diff --stat ${NEW_COMMIT}..${OLD_COMMIT}
        $(cat /tmp/diff.txt | sed 's/^/    /' | head -c 55555)
        
        https://github.com/tektoncd/pruner/compare/${NEW_COMMIT}..${OLD_COMMIT}
        EOF
        
        git push -f origin actions/update/sources-tektoncd-pruner

        if [ "$(gh pr list --base tektoncd-pruner --head actions/update/sources-tektoncd-pruner --json url --jq 'length')" = "0" ]; then
          echo "creating PR..."
          gh pr create -B tektoncd-pruner -H actions/update/sources-tektoncd-pruner --label=automated --label=upstream --fill
        else
          echo "a PR already exists, editing..."
          gh pr edit --title "[bot] Update tektoncd-pruner from tektoncd/pruner to ${NEW_COMMIT}" --body "$(cat /tmp/diff.txt | sed 's/^/    /' | head -c 55555)"
        fi
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
