name: tekton-kueue
components:
  - name: kueue
    image-prefix: tekton-
    prefetch-input: |
      {"type": "gomod", "path": "upstream"}
branches:
  # uncomment below line to get nightly builds
  # - name: main
  - name: main
    versions:
      - next
